import React from 'react'

export default function UseStateButton() {



  return (
    <button>Test</button>
  )
}


{/* <div className="card">
<button onClick={() => setCount((count) => count + 1)}>
  count is {count}
</button>
</div> */}